[project]
name = "uni-api-fastapi"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "asyncpg",
  "pydantic-settings",
  "python-dotenv",
]

[tool.uvicorn]
factory = false
host = "0.0.0.0"
port = 8000
