"""
The model, with no framework and no I/O in it.

Everything in this package can be imported and exercised without a database, a
web server or an event loop. Every other layer may depend on it; it depends on
none of them.
"""
